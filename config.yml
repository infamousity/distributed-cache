common:
  cache:
    log:
      level: info
    size: 1_073_741_824
    api:
      bind_port: 9090
    cluster:
      tls:
        enabled: false
      memberlist:
        node_name: "{{ hostname }}"
        bind_address: "{{ ip }}"
        bind_port: 8946
        advertise_addr: "{{ hostname }}"
        advertise_port: 8946
